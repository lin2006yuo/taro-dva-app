export default {
    namespace: 'products',
    state: [],
    reducers: {
        // 'delete'(state, { payload: id }) {
        //     console.log(11)
        //     return state.filter(item => item.id !== id)
        // }
    }
}